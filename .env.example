# Cloudflare Web Analytics beacon token (WR-01).
# Leave blank locally — the beacon <script> is then NOT emitted, so no
# requests fire with a placeholder token. Set the real value in the GitHub
# Actions environment (repo Settings → Secrets and variables → Actions) at
# launch. PUBLIC_ prefix is required so Astro exposes it to templates.
PUBLIC_CF_BEACON_TOKEN=
